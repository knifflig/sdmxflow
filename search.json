{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"Overview","text":"<p><code>sdmxflow</code> is a Python SDMX ingestion library that produces deterministic, append-only artifacts for data warehouse ETL/ELT workflows: a facts CSV, a metadata history trail, and exported codelists.</p> <p>It is built for data engineers and analytics engineers who need scheduled refresh semantics, reproducible artifacts, and reference data exports — not ad-hoc interactive SDMX exploration.</p> <p>Status Early but functional. The artifact contract is stable and designed to remain consistent as additional providers are added.</p> <p>Provider support (today): Eurostat (<code>source_id=\"ESTAT\"</code>). See Provider Support for details and differences.</p>","path":["Overview"],"tags":[]},{"location":"#what-you-get-artifact-contract","level":2,"title":"What you get (artifact contract)","text":"<p>Given an <code>out_dir</code>, <code>sdmxflow</code> writes:</p> <ul> <li><code>dataset.csv</code> — append-only facts across upstream versions, tagged by a leading <code>last_updated</code> column</li> <li><code>metadata.json</code> — operational metadata + an append-only version history</li> <li><code>codelists/</code> — reference CSVs (<code>code,name</code>) used to interpret coded dataset columns</li> </ul> <p>Example output tree:</p> <pre><code>&lt;out_dir&gt;/\n    dataset.csv\n    metadata.json\n    codelists/\n        &lt;CODELIST_ID&gt;.csv\n    logs/                # only when save_logs=True\n        &lt;agency&gt;__&lt;dataset&gt;__&lt;timestamp&gt;.log\n</code></pre>","path":["Overview"],"tags":[]},{"location":"#quickstart-minimal","level":2,"title":"Quickstart (minimal)","text":"<pre><code>from pathlib import Path\n\nfrom sdmxflow import SdmxDataset\n\nds = SdmxDataset(\n    out_dir=Path(\"./out/lfsa_egai2d\"),\n    source_id=\"ESTAT\",\n    dataset_id=\"lfsa_egai2d\",\n    save_logs=True,  # writes &lt;out_dir&gt;/logs/&lt;agency&gt;__&lt;dataset&gt;__&lt;timestamp&gt;.log\n)\n\nresult = ds.fetch()\nprint(\"appended:\", result.appended)\n</code></pre>","path":["Overview"],"tags":[]},{"location":"#how-refresh-works-workflow","level":2,"title":"How refresh works (workflow)","text":"<p>This diagram is intentionally identical to the README and describes the production workflow <code>fetch()</code> implements.</p> <pre><code>graph TD\n    A[\"Scheduled job&lt;br/&gt;cron / Airflow / Prefect\"] --&gt; B[\"Fetch upstream last-updated&lt;br/&gt;SDMX annotations (Eurostat)\"]\n    B --&gt; C{\"Local metadata.json&lt;br/&gt;exists?\"}\n    C -- Yes --&gt; D{\"Upstream&lt;br/&gt;changed?\"}\n    C -- No --&gt; E0\n\n    D -- No --&gt; G[\"No new version&lt;br/&gt;Keep dataset.csv&lt;br/&gt;Ensure metadata + codelists\"]\n    D -- Yes --&gt; E1\n\n    subgraph DL[\" \"]\n        direction LR\n        E1[\"Download new slice\"]\n        E0[\"Download initial slice\"]\n    end\n    style DL fill:transparent,stroke:transparent\n\n    E0 --&gt; F[\"Append rows to dataset.csv&lt;br/&gt;append-only, adds last_updated column\"]\n    E1 --&gt; F\n    F --&gt; H[\"Update metadata history&lt;br/&gt;Export codelists\"]\n    G --&gt; I[\"Warehouse ingestion step&lt;br/&gt;dbt / COPY / load job\"]\n    H --&gt; I</code></pre>","path":["Overview"],"tags":[]},{"location":"#docs-map-where-to-go-next","level":2,"title":"Docs map (where to go next)","text":"<ul> <li>New here? Start with Getting Started.</li> <li>Need file semantics? Read Output Artifacts (Contract).</li> <li>Deploying on a schedule? See Scheduling &amp; Deployment.</li> <li>Loading into a warehouse? See Integration Patterns.</li> <li>Looking for parameters and defaults? See Configuration Reference.</li> <li>Provider behavior and roadmap: Provider Support.</li> <li>Operational issues: FAQ &amp; Troubleshooting.</li> <li>Release history: Changelog (or https://github.com/knifflig/sdmxflow/releases)</li> </ul>","path":["Overview"],"tags":[]},{"location":"api/","level":1,"title":"Configuration Reference","text":"<p>This page documents the current user-facing configuration surface: <code>SdmxDataset</code> parameters, defaults, outputs, logging, and error behavior.</p>","path":["Reference","Configuration Reference"],"tags":[]},{"location":"api/#sdmxdataset","level":2,"title":"<code>SdmxDataset</code>","text":"<p><code>SdmxDataset</code> is the primary entrypoint.</p>","path":["Reference","Configuration Reference"],"tags":[]},{"location":"api/#constructor","level":3,"title":"Constructor","text":"<pre><code>from pathlib import Path\n\nfrom sdmxflow import SdmxDataset\n\nds = SdmxDataset(\n\n    out_dir=Path(\"./out/my_dataset\"),\n    source_id=\"ESTAT\",\n    dataset_id=\"lfsa_egai2d\",\n)\n</code></pre> <p>Parameters:</p> <ul> <li><code>out_dir</code> (required, <code>str | Path</code>): output directory for all artifacts. It is expanded and resolved.</li> <li><code>source_id</code> (required, <code>str</code>): provider/source identifier.</li> <li><code>dataset_id</code> (required, <code>str</code>): dataset identifier within the provider.</li> </ul> <p>Optional parameters:</p> <ul> <li> <p><code>agency_id</code> (<code>str | None</code>, default: <code>None</code>)</p> </li> <li> <p>For <code>ESTAT</code>, <code>agency_id</code> defaults to the upper-cased <code>source_id</code>.</p> </li> <li> <p><code>key</code> (<code>str | dict[str, object] | None</code>, default: <code>\"\"</code>)</p> </li> <li> <p>Provider-specific dataset restriction.</p> </li> <li>For <code>ESTAT</code>, use <code>None</code> to request the full dataset. The empty string <code>\"\"</code> means “provider default” (which is currently also the full dataset).</li> <li>Dict keys are supported for Eurostat bulk downloads and are converted deterministically to a SDMX key string.</li> <li> <p><code>params</code> (<code>dict[str, object] | None</code>, default: <code>None</code>)</p> </li> <li> <p>Provider-specific passthrough parameters.</p> </li> <li>For Eurostat bulk CSV downloads, <code>sdmxflow</code> recognizes common SDMX 3.0 bulk params such as time-window filters.</li> <li> <p><code>logger</code> (<code>logging.Logger | None</code>, default: <code>None</code>)</p> </li> <li> <p>If omitted, <code>sdmxflow</code> uses a default library logger.</p> </li> <li> <p><code>save_logs</code> (<code>bool</code>, default: <code>False</code>)</p> </li> <li> <p>If <code>True</code>, writes a per-run log file under <code>&lt;out_dir&gt;/logs/</code>.</p> </li> </ul>","path":["Reference","Configuration Reference"],"tags":[]},{"location":"api/#output-paths","level":3,"title":"Output paths","text":"<p><code>sdmxflow</code> uses fixed names under <code>out_dir</code>:</p> <ul> <li><code>dataset.csv</code></li> <li><code>metadata.json</code></li> <li><code>codelists/</code></li> </ul> <p>See Output Artifacts (Contract).</p>","path":["Reference","Configuration Reference"],"tags":[]},{"location":"api/#setup","level":3,"title":"<code>setup()</code>","text":"<p>Creates the output directory structure (safe to call multiple times):</p> <ul> <li><code>&lt;out_dir&gt;/</code></li> <li><code>&lt;out_dir&gt;/codelists/</code></li> </ul> <p><code>fetch()</code> calls <code>setup()</code> internally.</p>","path":["Reference","Configuration Reference"],"tags":[]},{"location":"api/#fetch","level":3,"title":"<code>fetch()</code>","text":"<p>Runs one refresh cycle:</p> <ol> <li>Ensures local directories exist.</li> <li>Resolves the upstream “last updated” timestamp.</li> <li>Loads or initializes <code>metadata.json</code>.</li> <li>Appends a new slice only if upstream changed.</li> <li>Ensures codelists and updates metadata.</li> </ol> <p>Return value:</p> <ul> <li>a <code>FetchResult</code> pointing at artifact paths and a boolean <code>appended</code>.</li> </ul>","path":["Reference","Configuration Reference"],"tags":[]},{"location":"api/#refresh-semantics","level":4,"title":"Refresh semantics","text":"<ul> <li>On “no change”: <code>dataset.csv</code> is not modified, but <code>metadata.json</code> is updated (e.g., <code>last_fetched_at</code>) and codelists are ensured.</li> <li>On “changed”: a new slice is downloaded and appended to <code>dataset.csv</code> with a leading <code>last_updated</code> column value.</li> </ul>","path":["Reference","Configuration Reference"],"tags":[]},{"location":"api/#fetchresult","level":2,"title":"<code>FetchResult</code>","text":"<p><code>FetchResult</code> fields:</p> <ul> <li><code>out_dir</code>: output directory used for this fetch</li> <li><code>dataset_csv</code>: path to the facts CSV (<code>&lt;out_dir&gt;/dataset.csv</code>)</li> <li><code>metadata_json</code>: path to metadata (<code>&lt;out_dir&gt;/metadata.json</code>)</li> <li><code>codelists_dir</code>: path to codelists directory (<code>&lt;out_dir&gt;/codelists/</code>)</li> <li><code>appended</code>: whether a new upstream version was appended</li> </ul> <p>Example:</p> <pre><code>result = ds.fetch()\nif result.appended:\n    # trigger a warehouse load step\n    pass\n</code></pre>","path":["Reference","Configuration Reference"],"tags":[]},{"location":"api/#logging-configuration","level":2,"title":"Logging configuration","text":"<p><code>sdmxflow</code> uses Python’s standard <code>logging</code> module and does not configure handlers.</p> <p>Minimal configuration:</p> <pre><code>import logging\n\nlogging.basicConfig(level=logging.INFO)\n</code></pre> <p>At <code>INFO</code> level, each <code>fetch()</code> emits exactly three user-facing messages:</p> <ol> <li>intention (what will be fetched and where)</li> <li>decision (download vs already up to date)</li> <li>completion summary (paths to artifacts)</li> </ol> <p>For detailed diagnostics:</p> <pre><code>logging.basicConfig(level=logging.DEBUG)\n</code></pre> <p>For per-run capture:</p> <ul> <li><code>save_logs=True</code> writes <code>&lt;out_dir&gt;/logs/&lt;agency&gt;__&lt;dataset&gt;__&lt;timestamp&gt;.log</code>.</li> </ul> <p>See Logging.</p>","path":["Reference","Configuration Reference"],"tags":[]},{"location":"api/#timeouts-retries-and-network-settings","level":2,"title":"Timeouts, retries, and network settings","text":"<p>Current behavior:</p> <ul> <li><code>fetch()</code> does not expose a top-level <code>timeout_seconds</code> or retry policy.</li> <li>Some lower-level download components support timeouts internally, but they are not part of the stable public API yet.</li> </ul> <p>Operational guidance:</p> <ul> <li>implement retries/backoff in your scheduler (Airflow/Prefect/Kubernetes)</li> <li>use <code>save_logs=True</code> to capture diagnostics</li> </ul>","path":["Reference","Configuration Reference"],"tags":[]},{"location":"api/#errors-and-exceptions","level":2,"title":"Errors and exceptions","text":"<p><code>fetch()</code> raises typed <code>sdmxflow</code> errors for common operational failure modes:</p> <ul> <li><code>SdmxDownloadError</code>: unsupported provider or a download failed</li> <li><code>SdmxTimeoutError</code>: upstream request timed out</li> <li><code>SdmxUnreachableError</code>: DNS/connection failures</li> <li><code>SdmxInterruptedError</code>: user interruption (Ctrl+C)</li> </ul> <p>Troubleshooting guidance is in FAQ &amp; Troubleshooting.</p>","path":["Reference","Configuration Reference"],"tags":[]},{"location":"changelog/","level":1,"title":"Changelog","text":"<p>All notable changes to this project are documented here.</p> <p>The format is based on Keep a Changelog. This project adheres to Semantic Versioning.</p> <p>Canonical links:</p> <ul> <li>GitHub releases: https://github.com/knifflig/sdmxflow/releases</li> </ul>","path":["Reference","Changelog"],"tags":[]},{"location":"changelog/#unreleased","level":2,"title":"[Unreleased]","text":"","path":["Reference","Changelog"],"tags":[]},{"location":"changelog/#011-2026-03-03","level":2,"title":"0.1.1 - 2026-03-03","text":"","path":["Reference","Changelog"],"tags":[]},{"location":"changelog/#fixed","level":3,"title":"Fixed","text":"<ul> <li>Relaxed the <code>sdmx1</code> dependency upper bound to avoid a transitive <code>packaging&gt;=26</code> requirement introduced in <code>sdmx1==2.25.1</code>, which can make <code>sdmxflow</code> incompatible with orchestration stacks that currently pin <code>packaging&lt;25.1</code> (e.g. Prefect 3.6.x).</li> </ul>","path":["Reference","Changelog"],"tags":[]},{"location":"changelog/#010-2026-03-03","level":2,"title":"0.1.0 - 2026-03-03","text":"<p>Initial public release.</p>","path":["Reference","Changelog"],"tags":[]},{"location":"changelog/#added","level":3,"title":"Added","text":"<ul> <li>High-level, user-facing API via <code>SdmxDataset</code>:</li> <li>Configure a dataset (<code>source_id</code>, <code>dataset_id</code>, optional <code>agency_id</code>, <code>key</code>, <code>params</code>) and an output folder (<code>out_dir</code>).</li> <li>Run <code>fetch()</code> for a single refresh cycle.</li> <li>Append-only dataset materialization:</li> <li>Writes/maintains a single <code>dataset.csv</code> under <code>out_dir</code>.</li> <li>Ensures a leading <code>last_updated</code> column (UTC ISO-8601) so each row is tied to the upstream version it belongs to.</li> <li>Deterministic on-disk artifact layout designed for scheduled warehouse refresh jobs:</li> <li><code>dataset.csv</code> (facts)</li> <li><code>metadata.json</code> (operational metadata + version history)</li> <li><code>codelists/</code> (reference tables used to interpret coded columns)</li> <li>Provider support:</li> <li>Eurostat (<code>source_id=\"ESTAT\"</code>) as the initial supported source.</li> <li>Fast upstream change detection for Eurostat:</li> <li>Queries a last-updated timestamp and uses it to decide whether a new version needs downloading.</li> <li>User-friendly logging contract for dataset refresh:</li> <li><code>fetch()</code> emits exactly three <code>INFO</code> messages per call (intent → version decision → completion summary).</li> <li>All other detail is emitted at <code>DEBUG</code> to keep production logs readable.</li> <li>Optional per-run log capture:</li> <li><code>save_logs=True</code> writes a dedicated log file under <code>&lt;out_dir&gt;/logs/</code> for each <code>fetch()</code> run.</li> <li>File handler is attached/detached safely for the duration of the call.</li> <li>Robust download plumbing:</li> <li>Native SDMX REST downloader that streams responses to disk and writes a small <code>.meta.json</code> sidecar describing the HTTP response.</li> <li>Typed exceptions for common failure modes (timeouts, unreachable server, user interrupts, metadata errors).</li> <li>Project documentation:</li> <li>User-facing docs in <code>docs/</code> rendered with Zensical.</li> <li>GitHub Pages workflow to build and publish the site.</li> <li>Packaging and quality gates:</li> <li><code>pyproject.toml</code> with complete PEP 621 metadata for publishing.</li> <li>Ruff configured and repository kept lint/format clean.</li> <li>Pytest suite covering core behaviors and edge cases.</li> </ul>","path":["Reference","Changelog"],"tags":[]},{"location":"changelog/#changed","level":3,"title":"Changed","text":"<ul> <li>Established Apache License 2.0 licensing for <code>sdmxflow</code>.</li> <li>Added explicit credits/acknowledgements for upstream inspirations and dependencies, including SDMX tooling and documentation tooling.</li> </ul>","path":["Reference","Changelog"],"tags":[]},{"location":"changelog/#known-limitations","level":3,"title":"Known limitations","text":"<ul> <li>Provider coverage is intentionally narrow in 0.1.0: only Eurostat is supported.</li> <li><code>fetch()</code> is optimized for “refresh into stable artifacts” workflows, not for interactive SDMX exploration.</li> <li>Output format is focused on CSV + JSON sidecar metadata; additional export formats may be added in future versions.</li> </ul>","path":["Reference","Changelog"],"tags":[]},{"location":"concepts-and-design/","level":1,"title":"Concepts &amp; Design","text":"<p>This page explains the design choices behind <code>sdmxflow</code>: append-only artifacts, incremental refresh logic, and why the outputs are shaped for warehouse ingestion.</p>","path":["Workflows","Concepts &amp; Design"],"tags":[]},{"location":"concepts-and-design/#the-mental-model","level":2,"title":"The mental model","text":"<p><code>sdmxflow</code> is not a “query SDMX interactively” tool. It is a scheduled ingestion tool that turns an SDMX dataset into stable artifacts you can load repeatedly.</p> <p>Key idea: you don’t mutate history. You append a new upstream version as a new slice in <code>dataset.csv</code> and keep the lineage in <code>metadata.json</code>.</p>","path":["Workflows","Concepts &amp; Design"],"tags":[]},{"location":"concepts-and-design/#append-only-artifacts-and-why","level":2,"title":"Append-only artifacts (and why)","text":"<p><code>sdmxflow</code> writes an append-only facts file:</p> <ul> <li><code>dataset.csv</code> keeps all downloaded versions over time.</li> <li>Each row is tagged with a <code>last_updated</code> value (UTC ISO-8601) that identifies the upstream version it came from.</li> </ul> <p>Why this is useful:</p> <ul> <li>Reproducibility: older versions remain available.</li> <li>Idempotent scheduling: a run that finds no upstream change does not rewrite the facts.</li> <li>Warehouse-friendly: you can load once and query “latest version” using <code>last_updated</code>.</li> </ul> <p>Gotcha Append-only means duplicates across versions are expected. The same “business key” row may appear multiple times (one per upstream version). Your downstream models should filter to the latest <code>last_updated</code> (or build a snapshot dimension).</p>","path":["Workflows","Concepts &amp; Design"],"tags":[]},{"location":"concepts-and-design/#incremental-refresh-logic-what-changed-means","level":2,"title":"Incremental refresh logic (what “changed?” means)","text":"<p>A single <code>fetch()</code> run:</p> <ol> <li>Ensures the output folder exists.</li> <li>Queries the provider for an upstream “last updated” timestamp.</li> <li>Loads <code>metadata.json</code> if present (otherwise initializes it).</li> <li>Compares the upstream timestamp to the most recently recorded <code>last_updated_data_at</code>.</li> <li>If different: downloads a new dataset slice and appends it; then updates metadata and codelists.</li> <li>If unchanged: skips the dataset download/append, but still ensures metadata and codelists are up to date.</li> </ol> <p>In the current implementation:</p> <ul> <li>For <code>source_id=\"ESTAT\"</code>, upstream change detection uses Eurostat’s SDMX metadata (“annotations”) via <code>sdmxflow.query.last_updated_data.eurostat_last_updated()</code>.</li> <li>The comparison is strict string equality on the canonical UTC ISO-8601 timestamp stored in <code>metadata.json</code>.</li> </ul> <p>Operational note If the upstream provider republishes data without updating their “last updated” signal, <code>sdmxflow</code> will not append a new slice. That’s a provider semantics issue, not a local-state issue. See Provider Support.</p>","path":["Workflows","Concepts &amp; Design"],"tags":[]},{"location":"concepts-and-design/#metadata-history-lineage-reproducibility","level":2,"title":"Metadata history (lineage &amp; reproducibility)","text":"<p><code>metadata.json</code> is the audit trail for your ingestion:</p> <ul> <li>dataset identity (<code>agency_id</code>, <code>dataset_id</code>, <code>key</code>, <code>params</code>)</li> <li><code>created_at</code>, <code>last_fetched_at</code>, <code>last_updated_at</code>, <code>last_updated_data_at</code></li> <li><code>versions[]</code>: an append-only list of versions, with HTTP details and <code>rows_appended</code></li> <li><code>files</code>: relative paths for artifacts</li> <li><code>codelists[]</code>: mapping from dataset columns to exported codelist CSVs</li> </ul> <p>Why it exists:</p> <ul> <li>Lineage: where data came from (URL/status/headers when available)</li> <li>Governance: what changed and when</li> <li>Debuggability: what a run did without digging through logs</li> </ul>","path":["Workflows","Concepts &amp; Design"],"tags":[]},{"location":"concepts-and-design/#codelists-and-dataset-dimensions","level":2,"title":"Codelists and dataset dimensions","text":"<p>Many SDMX datasets encode dimensions/attributes as short codes (e.g., <code>SEX=M</code>). To interpret or join those columns in a warehouse model, you need the corresponding code → label tables.</p> <p><code>sdmxflow</code>:</p> <ul> <li>downloads SDMX structures (Dataflow + DSD)</li> <li>extracts the codelists referenced by dimensions/attributes</li> <li>writes one CSV per codelist under <code>codelists/</code> with columns <code>code,name</code></li> <li>stores a column → codelist mapping in <code>metadata.json</code></li> </ul> <p>Practical implication: treat <code>codelists/*.csv</code> as reference tables in your warehouse.</p>","path":["Workflows","Concepts &amp; Design"],"tags":[]},{"location":"concepts-and-design/#deterministic-artifacts-principles-and-gotchas","level":2,"title":"Deterministic artifacts (principles and gotchas)","text":"<p><code>sdmxflow</code> aims for stable, deterministic outputs:</p> <ul> <li>Stable filenames (<code>dataset.csv</code>, <code>metadata.json</code>, <code>codelists/&lt;ID&gt;.csv</code>)</li> <li>Stable “slice tagging” via the <code>last_updated</code> column</li> <li>Strict schema matching when appending: if the provider CSV columns change, <code>sdmxflow</code> raises an error instead of silently corrupting the append history.</li> </ul> <p>Warning If the provider changes the dataset schema (columns), appends will fail with a metadata/schema error. In that case, treat it as a breaking upstream change and rebuild into a fresh <code>out_dir</code> (see FAQ &amp; Troubleshooting).</p>","path":["Workflows","Concepts &amp; Design"],"tags":[]},{"location":"concepts-and-design/#glossary-sdmx-terms","level":2,"title":"Glossary (SDMX terms)","text":"<ul> <li>SDMX: Statistical Data and Metadata eXchange, a standard for statistical data exchange.</li> <li>Dataflow: A dataset definition/endpoint identifier in SDMX.</li> <li>DSD (Data Structure Definition): The schema describing dimensions, attributes, measures.</li> <li>Dimension: A column used to identify a slice of data (e.g., time, geography).</li> <li>Attribute: Additional descriptor columns (may also use codelists).</li> <li>Codelist: Reference list mapping a code to a human-readable label.</li> <li>Last updated: Provider-specific signal for when data changed; used by <code>sdmxflow</code> to decide whether to append.</li> </ul> <p>Next:</p> <ul> <li>See Output Artifacts (Contract) for the stable on-disk contract.</li> <li>See Integration Patterns for warehouse loading examples.</li> </ul>","path":["Workflows","Concepts &amp; Design"],"tags":[]},{"location":"credits/","level":1,"title":"Credits &amp; License","text":"<p>This page lists acknowledgements for influences and upstream dependencies, plus the project license.</p>","path":["Credits &amp; License"],"tags":[]},{"location":"credits/#credits","level":2,"title":"Credits","text":"<ul> <li>The early direction for warehouse-friendly SDMX refresh workflows was influenced by a <code>dlt</code> SDMX extension gist shared by Martin Salo.</li> <li>Core SDMX protocol/model handling is powered by the <code>sdmx1</code> Python package.</li> </ul>","path":["Credits &amp; License"],"tags":[]},{"location":"credits/#documentation-tooling","level":2,"title":"Documentation tooling","text":"<p>This documentation site is built with Zensical:</p> <ul> <li>Project: https://zensical.org/</li> <li>Source: https://github.com/zensical/zensical</li> <li>License: MIT</li> </ul>","path":["Credits &amp; License"],"tags":[]},{"location":"credits/#license","level":2,"title":"License","text":"<p><code>sdmxflow</code> is licensed under the Apache License, Version 2.0.</p> <p>See https://github.com/knifflig/sdmxflow/blob/main/LICENSE.md.</p>","path":["Credits &amp; License"],"tags":[]},{"location":"development/","level":1,"title":"Development &amp; Contributing","text":"<p>This page explains how to work on <code>sdmxflow</code> locally: dev environment, tests/lint, docs preview, and how to add provider support in a disciplined way.</p>","path":["Development &amp; Contributing"],"tags":[]},{"location":"development/#local-dev-setup","level":2,"title":"Local dev setup","text":"<p>This project uses <code>uv</code> for development.</p> <pre><code>uv sync --group dev\n</code></pre>","path":["Development &amp; Contributing"],"tags":[]},{"location":"development/#run-tests","level":2,"title":"Run tests","text":"<pre><code>uv run pytest\n</code></pre>","path":["Development &amp; Contributing"],"tags":[]},{"location":"development/#lint-and-format","level":2,"title":"Lint and format","text":"<pre><code>uv run ruff check .\nuv run ruff format .\n</code></pre>","path":["Development &amp; Contributing"],"tags":[]},{"location":"development/#docs-preview-zensical","level":2,"title":"Docs preview (Zensical)","text":"<p>Preview locally:</p> <pre><code>uv run zensical serve\n</code></pre> <p>Build the static site:</p> <pre><code>uv run zensical build --clean\n</code></pre>","path":["Development &amp; Contributing"],"tags":[]},{"location":"development/#repository-structure-high-level","level":2,"title":"Repository structure (high level)","text":"<ul> <li><code>sdmxflow/dataset.py</code>: user-facing <code>SdmxDataset</code> entrypoint and refresh workflow</li> <li><code>sdmxflow/download/</code>: provider download implementations</li> <li><code>sdmxflow/query/</code>: upstream metadata queries (e.g., “last updated”)</li> <li><code>sdmxflow/metadata/</code>: metadata schema + read/write helpers</li> <li><code>sdmxflow/extract/</code>: structure/codelist extraction</li> <li><code>tests/</code>: unit tests (provider branches, error classification, contracts)</li> </ul>","path":["Development &amp; Contributing"],"tags":[]},{"location":"development/#how-to-add-a-provider-implementation-outline","level":2,"title":"How to add a provider (implementation outline)","text":"<p>Provider support should preserve the stable artifact contract:</p> <ul> <li><code>dataset.csv</code> with leading <code>last_updated</code></li> <li><code>metadata.json</code> with append-only version history</li> <li><code>codelists/</code> extraction and mapping</li> </ul> <p>Acceptance criteria for a new provider:</p> <ol> <li> <p>Change detection</p> </li> <li> <p>Implement an upstream “last updated” resolver for the provider.</p> </li> <li>Add unit tests for changed vs unchanged behavior.</li> <li> <p>Download and materialization</p> </li> <li> <p>Implement a downloader that writes a provider slice to a CSV without the internal <code>last_updated</code> column.</p> </li> <li>Ensure <code>append_version_slice()</code> can tag it and append deterministically.</li> <li> <p>Structures and codelists</p> </li> <li> <p>Ensure structures can be downloaded and codelists can be mapped from dataset columns.</p> </li> <li> <p>Error classification</p> </li> <li> <p>Timeouts, unreachable errors, and interruptions should raise typed <code>sdmxflow</code> errors.</p> </li> <li> <p>Docs update</p> </li> <li> <p>Add the provider to Provider Support and describe any provider-specific caveats.</p> </li> </ol>","path":["Development &amp; Contributing"],"tags":[]},{"location":"development/#pr-expectations","level":2,"title":"PR expectations","text":"<ul> <li>Keep changes focused and additive.</li> <li>Update docs when behavior changes.</li> <li>Add tests for new branches and error cases.</li> </ul> <p>See the repository contributing guide for details:</p> <ul> <li>https://github.com/knifflig/sdmxflow/blob/main/CONTRIBUTING.md</li> </ul>","path":["Development &amp; Contributing"],"tags":[]},{"location":"development/#release-notes-pypi","level":2,"title":"Release notes (PyPI)","text":"<p>Publishing to PyPI is automated via GitHub Actions on version tags matching <code>v*</code> (e.g. <code>v0.1.1</code>).</p> <p>The publish workflow validates that the tag version matches <code>pyproject.toml</code> before building and publishing.</p> <p>Prerequisite: configure PyPI “Trusted Publishing” (OIDC) for the GitHub repository so the publish step can upload without storing an API token in GitHub secrets.</p>","path":["Development &amp; Contributing"],"tags":[]},{"location":"examples/","level":1,"title":"Examples","text":"<p>This page provides runnable end-to-end examples (live network) that produce the full artifact layout and are suitable for copy/paste into scheduled jobs.</p>","path":["Reference","Examples"],"tags":[]},{"location":"examples/#cli-demo-live-network","level":2,"title":"CLI demo (live network)","text":"<p>If you want a runnable, end-to-end example (good for trying <code>sdmxflow</code> the first time or referencing from blog posts / README snippets), use the script in:</p> <ul> <li><code>scripts/download_dataset.py</code></li> </ul> <p>This script performs live network I/O against the provider and writes a stable on-disk artifact layout (dataset CSV + metadata + codelists).</p>","path":["Reference","Examples"],"tags":[]},{"location":"examples/#run-from-this-repository","level":3,"title":"Run from this repository","text":"<pre><code>uv sync --group dev\nuv run python scripts/download_dataset.py --help\n</code></pre> <p>Download the default Eurostat example dataset into <code>./out/estat/lfsa_egai2d</code>:</p> <pre><code>uv run python scripts/download_dataset.py\n</code></pre> <p>Write a per-run log file under <code>&lt;out_dir&gt;/logs/</code> (useful for debugging provider behavior):</p> <pre><code>uv run python scripts/download_dataset.py --save-logs\n</code></pre> <p>Pick an explicit output folder and increase verbosity:</p> <pre><code>uv run python scripts/download_dataset.py \\\n  --out-dir ./out/lfsa_egai2d \\\n  --log-level DEBUG \\\n  --save-logs\n</code></pre>","path":["Reference","Examples"],"tags":[]},{"location":"examples/#what-to-expect","level":3,"title":"What to expect","text":"<ul> <li>Console logs at the level you chose via <code>--log-level</code>.</li> <li>Output artifacts under your <code>--out-dir</code> (see Output layout).</li> </ul> <p>Next:</p> <ul> <li>See Output Artifacts (Contract) for the artifact contract.</li> <li>See Scheduling &amp; Deployment for orchestration patterns.</li> <li>See Integration Patterns for warehouse loading examples.</li> </ul>","path":["Reference","Examples"],"tags":[]},{"location":"faq/","level":1,"title":"FAQ &amp; Troubleshooting","text":"<p>This page lists common operational issues when running <code>sdmxflow</code> in scheduled jobs and how to debug them.</p>","path":["Reference","FAQ &amp; Troubleshooting"],"tags":[]},{"location":"faq/#no-new-data-appended","level":2,"title":"No new data appended","text":"<p>Symptoms:</p> <ul> <li><code>FetchResult.appended</code> is <code>False</code> repeatedly.</li> <li>Logs say “Already up to date; skipping download.”</li> </ul> <p>Likely causes:</p> <ul> <li>The upstream dataset has not changed.</li> <li>The provider’s “last updated” signal did not change even though the data changed.</li> </ul> <p>What to do:</p> <ul> <li>Enable <code>DEBUG</code> logging and re-run.</li> <li>Inspect <code>metadata.json</code> → <code>last_updated_data_at</code> and <code>versions[]</code>.</li> <li>Compare to the provider’s UI/metadata if available.</li> </ul> <p>See Logging and Provider Support.</p>","path":["Reference","FAQ &amp; Troubleshooting"],"tags":[]},{"location":"faq/#output-folder-already-exists","level":2,"title":"Output folder already exists","text":"<p>That’s expected.</p> <ul> <li><code>fetch()</code> will reuse the same <code>out_dir</code>.</li> <li><code>dataset.csv</code> is appended only when the upstream version changes.</li> <li><code>metadata.json</code> is updated on every run (<code>last_fetched_at</code> always bumps).</li> </ul>","path":["Reference","FAQ &amp; Troubleshooting"],"tags":[]},{"location":"faq/#large-dataset-performance-tips","level":2,"title":"Large dataset performance tips","text":"<ul> <li>Prefer running on a machine with fast local disk for <code>out_dir</code>.</li> <li>Treat <code>dataset.csv</code> as a warehouse staging input; avoid repeatedly reading it end-to-end in downstream steps if you can load incrementally.</li> <li>Consider splitting “download” and “warehouse load” into separate steps so warehouse loads can be retried without re-downloading.</li> </ul>","path":["Reference","FAQ &amp; Troubleshooting"],"tags":[]},{"location":"faq/#network-failures-and-retries","level":2,"title":"Network failures and retries","text":"<p><code>sdmxflow</code> classifies common failures into typed exceptions (timeout/unreachable/interrupted), but it does not implement a global retry policy at the top-level API.</p> <p>Operational pattern:</p> <ul> <li>Implement retries in your scheduler (Airflow retries / Prefect retries / Kubernetes Job backoff).</li> <li>Use <code>save_logs=True</code> to capture per-run debug logs for postmortems.</li> </ul>","path":["Reference","FAQ &amp; Troubleshooting"],"tags":[]},{"location":"faq/#how-to-resetrebuild-artifacts-safely","level":2,"title":"How to reset/rebuild artifacts safely","text":"<p>Sometimes the safest response to upstream schema changes or local corruption is to rebuild from scratch.</p> <p>Recommended approach:</p> <ol> <li>Move the existing folder aside:</li> </ol> <pre><code>mv /data/sdmx/my_dataset /data/sdmx/my_dataset.backup.$(date +%Y%m%d%H%M%S)\n</code></pre> <ol> <li>Run <code>fetch()</code> again into a clean <code>out_dir</code>.</li> </ol> <p>Warning Rebuilding means you lose the local append history in <code>dataset.csv</code> and <code>metadata.json</code> for that dataset folder. Keep backups if you need auditability.</p>","path":["Reference","FAQ &amp; Troubleshooting"],"tags":[]},{"location":"faq/#common-errors","level":2,"title":"Common errors","text":"","path":["Reference","FAQ &amp; Troubleshooting"],"tags":[]},{"location":"faq/#unsupported-provider","level":3,"title":"Unsupported provider","text":"<p>Error:</p> <ul> <li><code>SdmxDownloadError: Unsupported source_id=... Only 'ESTAT' is implemented.</code></li> </ul> <p>Fix:</p> <ul> <li>Use <code>source_id=\"ESTAT\"</code> for now.</li> <li>See Provider Support for roadmap and contribution path.</li> </ul>","path":["Reference","FAQ &amp; Troubleshooting"],"tags":[]},{"location":"faq/#csv-schema-mismatch","level":3,"title":"CSV schema mismatch","text":"<p>Error (paraphrased):</p> <ul> <li>“CSV schema mismatch: source columns differ from destination columns”</li> </ul> <p>Meaning:</p> <ul> <li>The provider CSV header changed compared to what you previously stored in <code>dataset.csv</code>.</li> </ul> <p>Fix:</p> <ul> <li>Rebuild into a new <code>out_dir</code> (see “reset/rebuild” above), or pin the dataset/key/params so the schema is stable.</li> </ul>","path":["Reference","FAQ &amp; Troubleshooting"],"tags":[]},{"location":"getting-started/","level":1,"title":"Getting Started","text":"<p>This page gets you from “install” to a first successful fetch, and explains what changes on the second run (append-only, incremental refresh).</p>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#installation","level":2,"title":"Installation","text":"","path":["Getting Started"],"tags":[]},{"location":"getting-started/#python-versions","level":3,"title":"Python versions","text":"<p><code>sdmxflow</code> supports Python 3.11 and 3.12.</p>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#from-pypi","level":3,"title":"From PyPI","text":"<pre><code>pip install sdmxflow\n</code></pre> <p>If you use <code>uv</code>:</p> <pre><code>uv add sdmxflow\n</code></pre>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#from-source-contributors","level":3,"title":"From source (contributors)","text":"<pre><code>git clone https://github.com/knifflig/sdmxflow\ncd sdmxflow\nuv sync --group dev\n</code></pre>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#minimal-working-example","level":2,"title":"Minimal working example","text":"<p>The main entrypoint is <code>SdmxDataset</code>.</p> <pre><code>from pathlib import Path\n\nfrom sdmxflow import SdmxDataset\n\nds = SdmxDataset(\n    out_dir=Path(\"./out/lfsa_egai2d\"),\n    source_id=\"ESTAT\",\n    dataset_id=\"lfsa_egai2d\",\n)\n\nresult = ds.fetch()\nprint(\"appended:\", result.appended)\n</code></pre>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#parameters-what-they-mean","level":2,"title":"Parameters (what they mean)","text":"<ul> <li><code>out_dir</code> (required): where artifacts are written and persisted between runs.</li> <li><code>source_id</code> (required): the provider/source identifier (currently <code>\"ESTAT\"</code>).</li> <li><code>dataset_id</code> (required): dataset identifier within the provider.</li> </ul> <p>Common optional parameters:</p> <ul> <li><code>agency_id</code>: defaults to <code>source_id</code> for <code>ESTAT</code>.</li> <li><code>key</code>: provider-specific SDMX key restriction.<ul> <li>For <code>ESTAT</code>, use <code>None</code> to request the full dataset; <code>\"\"</code> means “provider default” (currently also the full dataset).</li> </ul> </li> <li><code>params</code>: provider-specific passthrough parameters (e.g., time window).</li> <li><code>save_logs=True</code>: writes a per-run debug log file under <code>&lt;out_dir&gt;/logs/</code>.</li> </ul> <p>For the full parameter reference and defaults, see Configuration Reference.</p>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#what-happens-on-first-run-vs-second-run","level":2,"title":"What happens on first run vs second run","text":"","path":["Getting Started"],"tags":[]},{"location":"getting-started/#first-successful-run","level":3,"title":"First successful run","text":"<ul> <li>No <code>metadata.json</code> exists yet, so <code>sdmxflow</code> initializes metadata.</li> <li>It fetches upstream “last updated” metadata.</li> <li>It downloads the dataset slice and creates <code>dataset.csv</code>.</li> <li>It writes <code>metadata.json</code> and exports <code>codelists/</code>.</li> </ul>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#second-and-later-runs","level":3,"title":"Second (and later) runs","text":"<p><code>sdmxflow</code> compares the upstream “last updated” timestamp to the latest locally recorded one:</p> <ul> <li>If unchanged: it skips the dataset download and does not append to <code>dataset.csv</code>.</li> <li>If changed: it downloads and appends a new slice and updates metadata/codelists.</li> </ul> <p>Important <code>dataset.csv</code> is append-only across versions. It is normal for the same “logical row” to appear multiple times across different <code>last_updated</code> values.</p>","path":["Getting Started"],"tags":[]},{"location":"getting-started/#expected-output-folder-tree","level":2,"title":"Expected output folder tree","text":"<p>After a successful fetch you should see:</p> <pre><code>&lt;out_dir&gt;/\n    dataset.csv\n    metadata.json\n    codelists/\n        &lt;CODELIST_ID&gt;.csv\n</code></pre> <p>If you enabled per-run log capture:</p> <pre><code>&lt;out_dir&gt;/logs/\n    &lt;agency&gt;__&lt;dataset&gt;__&lt;timestamp&gt;.log\n</code></pre> <p>Next:</p> <ul> <li>Read Output Artifacts (Contract) for file semantics and examples.</li> <li>See Scheduling &amp; Deployment for production patterns.</li> <li>See Integration Patterns for warehouse loading examples.</li> </ul>","path":["Getting Started"],"tags":[]},{"location":"glossary/","level":1,"title":"Glossary","text":"<p>This page defines SDMX and <code>sdmxflow</code> terms used throughout the documentation.</p>","path":["Reference","Glossary"],"tags":[]},{"location":"glossary/#sdmx-terms","level":2,"title":"SDMX terms","text":"<ul> <li>SDMX: Statistical Data and Metadata eXchange, a standard for exchanging statistical data.</li> <li>Dataflow: A dataset definition / endpoint identifier in SDMX.</li> <li>DSD (Data Structure Definition): Schema describing dimensions, attributes, and measures.</li> <li>Dimension: A column that identifies a slice of the data (e.g., time, geo).</li> <li>Attribute: Descriptor columns that provide additional context.</li> <li>Codelist: A reference list mapping a short code to a label.</li> </ul>","path":["Reference","Glossary"],"tags":[]},{"location":"glossary/#sdmxflow-terms","level":2,"title":"sdmxflow terms","text":"<ul> <li>Artifact contract: The stable on-disk layout and file semantics <code>sdmxflow</code> produces.</li> <li>Append-only: New upstream versions are appended; older versions remain.</li> <li>Slice: One downloaded upstream version of the dataset.</li> <li><code>last_updated</code>: The upstream version tag written into every fact row.</li> <li><code>metadata.json</code> versions: The append-only history of downloaded slices.</li> </ul> <p>Where to go next:</p> <ul> <li>Concepts &amp; Design</li> <li>Output Artifacts (Contract)</li> </ul>","path":["Reference","Glossary"],"tags":[]},{"location":"integration-patterns/","level":1,"title":"Integration Patterns","text":"<p>This page shows practical “fetch → load → model” patterns for common warehouse tools, including a small end-to-end example that joins fact rows to codelists.</p>","path":["Workflows","Integration Patterns"],"tags":[]},{"location":"integration-patterns/#warehouse-loading-basics","level":2,"title":"Warehouse loading basics","text":"<p><code>sdmxflow</code> produces files; it does not load into a warehouse directly.</p> <p>Typical flow:</p> <ol> <li>Run <code>fetch()</code> into a durable <code>out_dir</code>.</li> <li>Load <code>&lt;out_dir&gt;/dataset.csv</code> into a staging table.</li> <li>Load <code>&lt;out_dir&gt;/codelists/*.csv</code> into reference tables.</li> <li>Model “latest version” using the <code>last_updated</code> column.</li> </ol> <p>See Output Artifacts (Contract) for file semantics.</p>","path":["Workflows","Integration Patterns"],"tags":[]},{"location":"integration-patterns/#postgres-copy","level":2,"title":"Postgres (COPY)","text":"<pre><code>-- Create a wide staging table that matches the CSV header.\n-- (The provider columns vary by dataset, so define this based on the header.)\n\nCREATE TABLE IF NOT EXISTS sdmx_stage_lfsa_egai2d (\n  last_updated timestamptz NOT NULL,\n  -- provider columns...\n  -- geo text,\n  -- sex text,\n  -- time_period text,\n  -- value numeric,\n  raw_line text\n);\n\n-- COPY requires the server to see the file. In many deployments you will use\n-- \\copy from psql (client-side) or a file ingestion service.\n</code></pre> <p>Client-side load (psql <code>\\copy</code>):</p> <pre><code>psql \"$DATABASE_URL\" -c \"\\\\copy sdmx_stage_lfsa_egai2d FROM '/data/lfsa_egai2d/dataset.csv' WITH (FORMAT csv, HEADER true)\"\n</code></pre> <p>Note If you re-load the full append-only file each run, your staging table will accumulate duplicates. Consider loading incrementally by filtering new <code>last_updated</code> values (or truncate+reload staging, then build downstream models for “latest”).</p>","path":["Workflows","Integration Patterns"],"tags":[]},{"location":"integration-patterns/#duckdb-read_csv_auto","level":2,"title":"DuckDB (read_csv_auto)","text":"<p>DuckDB is convenient for local validation and “lightweight warehouse” pipelines:</p> <pre><code>from pathlib import Path\n\nimport duckdb\n\nout_dir = Path(\"./out/lfsa_egai2d\")\ncon = duckdb.connect()\n\ncon.execute(\n    \"\"\"\n    CREATE OR REPLACE TABLE facts AS\n    SELECT *\n    FROM read_csv_auto(?, header=true)\n    \"\"\",\n    [str(out_dir / \"dataset.csv\")],\n)\n\n# Example: select the newest upstream version present in the file.\nlatest = con.execute(\"SELECT max(last_updated) FROM facts\").fetchone()[0]\nprint(\"latest last_updated:\", latest)\n</code></pre>","path":["Workflows","Integration Patterns"],"tags":[]},{"location":"integration-patterns/#bigquery-snowflake-redshift-generic-pattern","level":2,"title":"BigQuery / Snowflake / Redshift (generic pattern)","text":"<p>For managed warehouses:</p> <ul> <li>stage the file in object storage (GCS/S3/Azure Blob)</li> <li>load into a staging table</li> <li>either:</li> <li>model latest version in SQL (<code>QUALIFY</code>/window function), or</li> <li>snapshot by <code>last_updated</code></li> </ul> <p>The key operational decision is whether your staging table is:</p> <ul> <li>truncate+reload (simple, robust), or</li> <li>incremental append (more efficient; requires tracking <code>last_updated</code> values already loaded)</li> </ul>","path":["Workflows","Integration Patterns"],"tags":[]},{"location":"integration-patterns/#dbt-pattern-staging-latest-view","level":2,"title":"dbt pattern (staging + latest view)","text":"<p>A common dbt approach:</p> <ul> <li>define <code>dataset.csv</code> as an external source (or load it to a raw table)</li> <li>create a staging model that picks the latest version</li> </ul> <p>Pseudo-SQL for “latest”:</p> <pre><code>WITH ranked AS (\n  SELECT\n    *,\n    row_number() OVER (\n      PARTITION BY /* your natural key columns here */\n      ORDER BY last_updated DESC\n    ) AS rn\n  FROM {{ source('sdmx', 'facts') }}\n)\nSELECT *\nFROM ranked\nWHERE rn = 1\n</code></pre>","path":["Workflows","Integration Patterns"],"tags":[]},{"location":"integration-patterns/#end-to-end-example-fetch-join-to-a-codelist","level":2,"title":"End-to-end example: fetch → join to a codelist","text":"<p>This example is intentionally generic: it demonstrates the mechanics without assuming a specific dataset schema.</p> <pre><code>from __future__ import annotations\n\nfrom pathlib import Path\n\nimport duckdb\n\nfrom sdmxflow import SdmxDataset\n\nout_dir = Path(\"./out/example\")\n\n# 1) Fetch artifacts.\nds = SdmxDataset(out_dir=out_dir, source_id=\"ESTAT\", dataset_id=\"lfsa_egai2d\")\nds.fetch()\n\n# 2) Load facts.\ncon = duckdb.connect()\ncon.execute(\n    \"CREATE OR REPLACE TABLE facts AS SELECT * FROM read_csv_auto(?, header=true)\",\n    [str(out_dir / \"dataset.csv\")],\n)\n\n# 3) Load one codelist CSV (pick one that exists).\n# In a real pipeline you would iterate `out_dir/codelists/*.csv`.\ncodelist_path = next((out_dir / \"codelists\").glob(\"*.csv\"))\ncon.execute(\n    \"CREATE OR REPLACE TABLE cl AS SELECT * FROM read_csv_auto(?, header=true)\",\n    [str(codelist_path)],\n)\n\nprint(\"codelist file:\", codelist_path.name)\nprint(con.execute(\"SELECT * FROM cl LIMIT 5\").fetchall())\n\n# 4) Join strategy: join facts.&lt;dimension_column&gt; = cl.code\n# The exact dimension column depends on your dataset and the codelist.\n</code></pre> <p>To make joins deterministic in production, use the column → codelist mapping stored in <code>metadata.json</code> (see Output Artifacts (Contract)).</p> <p>Next:</p> <ul> <li>See Scheduling &amp; Deployment for orchestration patterns.</li> <li>See Provider Support for provider semantics and differences.</li> </ul>","path":["Workflows","Integration Patterns"],"tags":[]},{"location":"logging/","level":1,"title":"Logging","text":"<p>This page documents how <code>sdmxflow</code> logs in production and how to capture debug logs for troubleshooting.</p> <p><code>sdmxflow</code> uses the standard library <code>logging</code> module and does not configure handlers.</p>","path":["Reference","Logging"],"tags":[]},{"location":"logging/#info-level-contract","level":2,"title":"INFO-level contract","text":"<p>At <code>INFO</code> level, <code>SdmxDataset.fetch()</code> emits exactly three user-facing messages per call:</p> <ol> <li>Fetch intention (what will be fetched and where)</li> <li>Version decision (download vs already up to date)</li> <li>Completion summary (artifact paths)</li> </ol> <p>All other detail is emitted at <code>DEBUG</code>.</p>","path":["Reference","Logging"],"tags":[]},{"location":"logging/#enabling-logs","level":2,"title":"Enabling logs","text":"<p>In applications, configure logging once at process startup:</p> <pre><code>import logging\n\nlogging.basicConfig(level=logging.INFO)\n</code></pre> <p>For troubleshooting:</p> <pre><code>logging.basicConfig(level=logging.DEBUG)\n</code></pre>","path":["Reference","Logging"],"tags":[]},{"location":"logging/#per-run-log-files","level":2,"title":"Per-run log files","text":"<p>If you pass <code>save_logs=True</code> to <code>SdmxDataset</code>, a per-run log file is written under:</p> <ul> <li><code>&lt;out_dir&gt;/logs/&lt;agency&gt;__&lt;dataset&gt;__&lt;timestamp&gt;.log</code></li> </ul> <p>The file handler is attached only for the duration of <code>fetch()</code> and is detached/closed in a <code>finally</code> block.</p> <p>Next:</p> <ul> <li>Configuration Reference</li> <li>FAQ &amp; Troubleshooting</li> </ul>","path":["Reference","Logging"],"tags":[]},{"location":"output-layout/","level":1,"title":"Output Artifacts (Contract)","text":"<p>This page defines the on-disk artifact contract <code>sdmxflow</code> produces: file names, semantics, and how to use them in a warehouse ingestion workflow.</p> <p>Artifact Contract (stable) Given an <code>out_dir</code>, <code>sdmxflow</code> writes:</p> <ul> <li><code>dataset.csv</code> (append-only facts; first column <code>last_updated</code>)</li> <li><code>metadata.json</code> (metadata history + operational fields)</li> <li><code>codelists/</code> (reference tables with <code>code,name</code>)</li> </ul> <p>Optional: <code>logs/</code> when <code>save_logs=True</code>.</p>","path":["Workflows","Output Artifacts (Contract)"],"tags":[]},{"location":"output-layout/#directory-layout","level":2,"title":"Directory layout","text":"<pre><code>&lt;out_dir&gt;/\n    dataset.csv\n    metadata.json\n    codelists/\n        &lt;CODELIST_ID&gt;.csv\n    logs/                     # only when save_logs=True\n        &lt;agency&gt;__&lt;dataset&gt;__&lt;timestamp&gt;.log\n</code></pre>","path":["Workflows","Output Artifacts (Contract)"],"tags":[]},{"location":"output-layout/#datasetcsv","level":2,"title":"<code>dataset.csv</code>","text":"","path":["Workflows","Output Artifacts (Contract)"],"tags":[]},{"location":"output-layout/#schema","level":3,"title":"Schema","text":"<p><code>dataset.csv</code> is the provider dataset schema with one additional leading column:</p> <ul> <li><code>last_updated</code> (string, UTC ISO-8601, e.g. <code>2026-03-03T00:00:00Z</code>)</li> </ul> <p>All remaining columns are provider-defined and depend on the dataset.</p> <p>Important <code>sdmxflow</code> does not deduplicate across versions. When a new upstream version is appended, it appends the full downloaded slice tagged with that version’s <code>last_updated</code>.</p>","path":["Workflows","Output Artifacts (Contract)"],"tags":[]},{"location":"output-layout/#example-rows","level":3,"title":"Example rows","text":"<p>Example (illustrative — provider columns vary):</p> <pre><code>last_updated,geo,sex,time_period,value\n2026-03-01T00:00:00Z,DE,M,2024-12,123.4\n2026-03-01T00:00:00Z,DE,F,2024-12,120.1\n2026-02-01T00:00:00Z,DE,M,2024-11,122.9\n</code></pre>","path":["Workflows","Output Artifacts (Contract)"],"tags":[]},{"location":"output-layout/#append-behavior","level":3,"title":"Append behavior","text":"<ul> <li>On the first successful fetch, <code>dataset.csv</code> is created with a header.</li> <li>On later fetches, <code>sdmxflow</code> appends rows only when the upstream <code>last_updated</code> timestamp changed.</li> <li>If the provider changes the CSV header (column names or ordering), append fails with a schema/metadata error instead of producing a corrupted mixed-schema history.</li> </ul>","path":["Workflows","Output Artifacts (Contract)"],"tags":[]},{"location":"output-layout/#how-to-use-in-a-warehouse","level":3,"title":"How to use in a warehouse","text":"<p>Common patterns:</p> <ul> <li>Truncate + reload staging: load the full append-only file each run, then model “latest” downstream.</li> <li>Incremental load: load only rows where <code>last_updated</code> is newer than what your warehouse already has.</li> </ul> <p>See Integration Patterns.</p>","path":["Workflows","Output Artifacts (Contract)"],"tags":[]},{"location":"output-layout/#metadatajson","level":2,"title":"<code>metadata.json</code>","text":"<p><code>metadata.json</code> is the operational and lineage record.</p> <p>Key semantics:</p> <ul> <li><code>last_fetched_at</code> updates on every fetch attempt (even if no new data appended).</li> <li><code>versions[]</code> is append-only and records one entry per appended upstream version.</li> <li><code>codelists[]</code> maps dataset columns to codelist files.</li> </ul>","path":["Workflows","Output Artifacts (Contract)"],"tags":[]},{"location":"output-layout/#example-structure-snippet","level":3,"title":"Example structure (snippet)","text":"<p>The exact file contains more fields; this snippet matches the current schema shape:</p> <pre><code>{\n  \"schema_version\": 1,\n  \"agency_id\": \"ESTAT\",\n  \"dataset_id\": \"lfsa_egai2d\",\n  \"created_at\": \"2026-03-03T01:02:03Z\",\n  \"last_fetched_at\": \"2026-03-03T02:00:00Z\",\n  \"last_updated_at\": \"2026-03-03T02:00:00Z\",\n  \"last_updated_data_at\": \"2026-03-01T00:00:00Z\",\n  \"files\": {\n    \"datasets\": {\"csv\": \"dataset.csv\"},\n    \"codelists\": {\n      \"sex\": \"codelists/CL_SEX.csv\"\n    }\n  },\n  \"versions\": [\n    {\n      \"agency_id\": \"ESTAT\",\n      \"created_at\": \"2026-03-03T02:00:00Z\",\n      \"last_updated_data_at\": \"2026-03-01T00:00:00Z\",\n      \"dataset\": {\"rows_appended\": 12345, \"last_updated_column\": \"last_updated\"},\n      \"http\": {\"url\": \"https://…\", \"status_code\": 200, \"headers\": {\"etag\": \"…\"}}\n    }\n  ],\n  \"codelists\": [\n    {\n      \"column_name\": \"sex\",\n      \"column_pos\": 2,\n      \"codelist_id\": \"CL_SEX\",\n      \"codelist_path\": \"codelists/CL_SEX.csv\",\n      \"codelist_kind\": \"dimension\",\n      \"codelist_type\": \"reference\",\n      \"codelist_labels\": {\"en\": \"Sex\"}\n    }\n  ]\n}\n</code></pre> <p>Note Timestamps are written as UTC ISO-8601 <code>...Z</code> strings.</p>","path":["Workflows","Output Artifacts (Contract)"],"tags":[]},{"location":"output-layout/#codelists","level":2,"title":"<code>codelists/</code>","text":"<p><code>codelists/</code> contains one CSV per used codelist, with a stable two-column schema:</p> <ul> <li><code>code</code> (string)</li> <li><code>name</code> (best-effort label, typically English if available)</li> </ul> <p>Example:</p> <pre><code>code,name\nM,Male\nF,Female\n</code></pre>","path":["Workflows","Output Artifacts (Contract)"],"tags":[]},{"location":"output-layout/#how-to-join-to-fact-columns","level":3,"title":"How to join to fact columns","text":"<ul> <li>Identify the fact column (dimension/attribute) you want to decode.</li> <li>Load the codelist CSV matching that column (use <code>metadata.json</code> → <code>codelists[]</code> mapping).</li> <li>Join <code>facts.&lt;column&gt;</code> = <code>codelist.code</code>.</li> </ul> <p>See Integration Patterns for examples.</p>","path":["Workflows","Output Artifacts (Contract)"],"tags":[]},{"location":"output-layout/#logs-save_logstrue","level":2,"title":"Logs (<code>save_logs=True</code>)","text":"<p>If you enabled per-run log capture, a log file is written under <code>&lt;out_dir&gt;/logs/</code>.</p> <p>See Logging for the INFO-level logging contract.</p>","path":["Workflows","Output Artifacts (Contract)"],"tags":[]},{"location":"provider-support/","level":1,"title":"Provider Support","text":"<p>This page documents which SDMX providers are supported today and how provider differences affect refresh semantics.</p>","path":["Reference","Provider Support"],"tags":[]},{"location":"provider-support/#current-support","level":2,"title":"Current support","text":"<ul> <li>Eurostat (<code>source_id=\"ESTAT\"</code>)</li> <li>Change detection uses Eurostat’s upstream “last updated” metadata.</li> <li>Data download uses Eurostat’s SDMX 3.0 bulk dissemination endpoint (CSV).</li> <li>Codelists are extracted from SDMX structures (Dataflow + DSD).</li> </ul> <p>Status Provider support is currently limited to Eurostat, but the project is designed to add additional sources over time. The public artifact contract is intended to remain stable across providers.</p>","path":["Reference","Provider Support"],"tags":[]},{"location":"provider-support/#planned-possible-future-work","level":2,"title":"Planned / possible future work","text":"<p>This is intentionally generic (not a commitment):</p> <ul> <li>additional SDMX sources via the SDMX protocol (national statistical offices, central banks, etc.)</li> <li>provider-specific optimizations (bulk endpoints, caching)</li> <li>richer metadata capture for governance</li> </ul>","path":["Reference","Provider Support"],"tags":[]},{"location":"provider-support/#provider-differences-what-can-vary","level":2,"title":"Provider differences (what can vary)","text":"<p>Across SDMX providers, these aspects can differ:</p> <ul> <li>Change detection signal: what “last updated” means and how reliable it is.</li> <li>Download mechanics: bulk CSV endpoints vs generic SDMX queries.</li> <li>Schema stability: some providers change column ordering or naming.</li> <li>Codelist availability: completeness and language coverage for labels.</li> </ul> <p><code>sdmxflow</code> tries to normalize what it controls (artifact layout, metadata schema), but it cannot force provider semantics.</p>","path":["Reference","Provider Support"],"tags":[]},{"location":"provider-support/#how-sdmxflow-detects-updates","level":2,"title":"How <code>sdmxflow</code> detects updates","text":"<p>At a high level:</p> <ul> <li>Fetch upstream <code>last_updated</code>.</li> <li>Compare to the latest locally recorded version in <code>metadata.json</code>.</li> <li>Append only when the timestamp differs.</li> </ul> <p>See Concepts &amp; Design for the full workflow.</p>","path":["Reference","Provider Support"],"tags":[]},{"location":"provider-support/#how-to-add-a-provider-high-level","level":2,"title":"How to add a provider (high-level)","text":"<p>Provider support is implemented in the Python package (not in docs config). The rough steps:</p> <ol> <li>Implement an upstream “last updated” query for the provider.</li> <li>Implement a dataset downloader that materializes a provider dataset slice as a CSV.</li> <li>Wire the provider into <code>SdmxDataset.fetch()</code> (dispatch by <code>source_id</code>).</li> <li>Ensure codelists/structures can be downloaded and mapped to dataset columns.</li> <li>Add tests that cover:</li> <li>changed vs unchanged upstream timestamps</li> <li>schema mismatch behavior</li> <li>error classification (timeout/unreachable/interrupted)</li> </ol> <p>For contributor expectations and repo tooling, see Development &amp; Contributing.</p>","path":["Reference","Provider Support"],"tags":[]},{"location":"scheduling-and-deployment/","level":1,"title":"Scheduling &amp; Deployment","text":"<p>This page shows production-style patterns for running <code>sdmxflow</code> on a schedule (cron, Airflow, Prefect) and operational practices for reliable SDMX ingestion.</p>","path":["Workflows","Scheduling &amp; Deployment"],"tags":[]},{"location":"scheduling-and-deployment/#the-baseline-pattern","level":2,"title":"The baseline pattern","text":"<p>A job run should look like:</p> <ol> <li>Call <code>SdmxDataset(...).fetch()</code></li> <li>Load/update your warehouse using the artifacts (<code>dataset.csv</code>, <code>codelists/</code>, <code>metadata.json</code>)</li> <li>Emit logs/metrics based on <code>FetchResult.appended</code></li> </ol> <p>Recommendation Treat <code>out_dir</code> as durable state. Put it on persistent storage (VM disk, network volume, object store sync target) so your scheduler can run stateless workers.</p>","path":["Workflows","Scheduling &amp; Deployment"],"tags":[]},{"location":"scheduling-and-deployment/#cron-simple-and-effective","level":2,"title":"Cron (simple and effective)","text":"<p>Create a small Python entrypoint (recommended):</p> <pre><code># refresh_dataset.py\nfrom pathlib import Path\n\nfrom sdmxflow import SdmxDataset\n\n\ndef main() -&gt; None:\n    ds = SdmxDataset(\n        out_dir=Path(\"/var/lib/sdmxflow/lfsa_egai2d\"),\n        source_id=\"ESTAT\",\n        dataset_id=\"lfsa_egai2d\",\n        save_logs=True,\n    )\n    result = ds.fetch()\n    # Use `result.appended` for downstream behavior (e.g., trigger a load step).\n    print(f\"appended={result.appended}\")\n\n\nif __name__ == \"__main__\":\n    main()\n</code></pre> <p>Cron entry (daily at 02:00 UTC):</p> <pre><code>0 2 * * * /usr/bin/python3 /opt/jobs/refresh_dataset.py &gt;&gt; /var/log/sdmxflow.log 2&gt;&amp;1\n</code></pre>","path":["Workflows","Scheduling &amp; Deployment"],"tags":[]},{"location":"scheduling-and-deployment/#airflow","level":2,"title":"Airflow","text":"","path":["Workflows","Scheduling &amp; Deployment"],"tags":[]},{"location":"scheduling-and-deployment/#pythonoperator","level":3,"title":"PythonOperator","text":"<pre><code>from __future__ import annotations\n\nfrom pathlib import Path\nfrom datetime import datetime, timezone\n\nfrom airflow import DAG\nfrom airflow.operators.python import PythonOperator\n\nfrom sdmxflow import SdmxDataset\n\n\ndef refresh() -&gt; bool:\n    ds = SdmxDataset(\n        out_dir=Path(\"/data/sdmx/lfsa_egai2d\"),\n        source_id=\"ESTAT\",\n        dataset_id=\"lfsa_egai2d\",\n    )\n    result = ds.fetch()\n    return result.appended\n\n\nwith DAG(\n    dag_id=\"sdmxflow_refresh\",\n    schedule=\"0 2 * * *\",\n    start_date=datetime(2026, 1, 1, tzinfo=timezone.utc),\n    catchup=False,\n) as dag:\n    refresh_task = PythonOperator(task_id=\"refresh\", python_callable=refresh)\n</code></pre>","path":["Workflows","Scheduling &amp; Deployment"],"tags":[]},{"location":"scheduling-and-deployment/#taskflow-api","level":3,"title":"TaskFlow API","text":"<pre><code>from __future__ import annotations\n\nfrom pathlib import Path\n\nfrom airflow.decorators import dag, task\n\nfrom sdmxflow import SdmxDataset\n\n\n@task\ndef refresh() -&gt; bool:\n    ds = SdmxDataset(\n        out_dir=Path(\"/data/sdmx/lfsa_egai2d\"),\n        source_id=\"ESTAT\",\n        dataset_id=\"lfsa_egai2d\",\n    )\n    return ds.fetch().appended\n\n\n@dag(schedule=\"0 2 * * *\", catchup=False)\ndef sdmxflow_refresh():\n    appended = refresh()\n    # Use `appended` to branch into a downstream load task.\n\n\nsdmxflow_refresh()\n</code></pre>","path":["Workflows","Scheduling &amp; Deployment"],"tags":[]},{"location":"scheduling-and-deployment/#prefect","level":2,"title":"Prefect","text":"<p><code>sdmxflow</code> does not require Prefect, but it composes well:</p> <pre><code>from __future__ import annotations\n\nfrom pathlib import Path\n\nfrom prefect import flow, task\n\nfrom sdmxflow import SdmxDataset\n\n\n@task(retries=3, retry_delay_seconds=60)\ndef refresh() -&gt; bool:\n    ds = SdmxDataset(\n        out_dir=Path(\"/data/sdmx/lfsa_egai2d\"),\n        source_id=\"ESTAT\",\n        dataset_id=\"lfsa_egai2d\",\n        save_logs=True,\n    )\n    return ds.fetch().appended\n\n\n@flow\ndef refresh_flow() -&gt; None:\n    appended = refresh()\n    if appended:\n        # trigger warehouse load\n        pass\n\n\nif __name__ == \"__main__\":\n    refresh_flow()\n</code></pre>","path":["Workflows","Scheduling &amp; Deployment"],"tags":[]},{"location":"scheduling-and-deployment/#docker-wrapping-pattern","level":2,"title":"Docker (wrapping pattern)","text":"<p>This repository does not ship an official container image. The typical pattern is:</p> <ul> <li>build a thin image that installs <code>sdmxflow</code></li> <li>mount a persistent volume to the container at the <code>out_dir</code></li> <li>run a small entrypoint script</li> </ul> <p>Example <code>Dockerfile</code>:</p> <pre><code>FROM python:3.12-slim\n\nRUN pip install --no-cache-dir sdmxflow\n\nWORKDIR /app\nCOPY refresh_dataset.py /app/refresh_dataset.py\n\nCMD [\"python\", \"/app/refresh_dataset.py\"]\n</code></pre> <p>Run (mount persistent artifacts):</p> <p>Note In your container entrypoint, set <code>out_dir</code> to a path inside the mounted volume (e.g. <code>/data/lfsa_egai2d</code>).</p> <pre><code>docker run --rm \\\n  -v /srv/sdmxflow/lfsa_egai2d:/data/lfsa_egai2d \\\n  my-sdmxflow-job:latest\n</code></pre>","path":["Workflows","Scheduling &amp; Deployment"],"tags":[]},{"location":"scheduling-and-deployment/#operational-practices","level":2,"title":"Operational practices","text":"","path":["Workflows","Scheduling &amp; Deployment"],"tags":[]},{"location":"scheduling-and-deployment/#retries-and-idempotency","level":3,"title":"Retries and idempotency","text":"<ul> <li><code>fetch()</code> is designed to be safe to run repeatedly.</li> <li>There is no built-in exponential backoff at the top-level API today; implement retries in your scheduler (Airflow/Pefect/K8s Jobs).</li> </ul>","path":["Workflows","Scheduling &amp; Deployment"],"tags":[]},{"location":"scheduling-and-deployment/#logging","level":3,"title":"Logging","text":"<ul> <li>At <code>INFO</code>, each fetch emits exactly 3 messages (intent, decision, completion summary).</li> <li>Use <code>DEBUG</code> for diagnostics.</li> <li>Use <code>save_logs=True</code> for per-run log capture under <code>&lt;out_dir&gt;/logs/</code>.</li> </ul> <p>See Configuration Reference and Logging.</p>","path":["Workflows","Scheduling &amp; Deployment"],"tags":[]},{"location":"scheduling-and-deployment/#storage-and-permissions","level":3,"title":"Storage and permissions","text":"<ul> <li>Put <code>out_dir</code> on durable storage.</li> <li>Ensure the job user can create/write: <code>dataset.csv</code>, <code>metadata.json</code>, <code>codelists/</code>, and optionally <code>logs/</code>.</li> </ul>","path":["Workflows","Scheduling &amp; Deployment"],"tags":[]},{"location":"scheduling-and-deployment/#when-to-trigger-warehouse-loads","level":3,"title":"When to trigger warehouse loads","text":"<p>Use <code>FetchResult.appended</code>:</p> <ul> <li><code>True</code>: new facts were appended → run your load job.</li> <li><code>False</code>: upstream unchanged → you might skip loading (but metadata/codelists may still be updated).</li> </ul> <p>Next:</p> <ul> <li>See Integration Patterns for load examples.</li> </ul>","path":["Workflows","Scheduling &amp; Deployment"],"tags":[]}]}